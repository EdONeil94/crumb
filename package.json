{
  "name": "crumbz-app",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:prod": "E2E_EMULATOR=0 playwright test",
    "emulators": "firebase emulators:start --only auth,firestore,storage --project crumb-ddeb6",
    "check:dead-refs": "node scripts/check-dead-refs.js",
    "cleanup:e2e": "node scripts/cleanup-e2e-data.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "firebase-admin": "^14.3.0",
    "firebase-tools": "^15.28.2",
    "vite": "^8.2.0"
  },
  "dependencies": {
    "firebase": "^12.18.0"
  }
}
